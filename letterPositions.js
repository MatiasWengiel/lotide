const letterPositions = function (sentence) {
  const lowerCaseNoSpaces = sentence.split(" ").join("").toLowerCase();
  console.log(lowerCaseNoSpaces);
  const results = {};

  const indexFinder = function (lowerCaseNoSpaces) {
    // will find the index of a letter, then push it to results along with each index in which the letter occurs"
  }

  return results;
}

console.log(letterPositions("Hello world"));